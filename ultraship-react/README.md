# Ultraship React Frontend (Skeleton)

React + Vite + Apollo Client app that connects to the Spring Boot GraphQL backend.

## Run

```bash
npm install
npm run dev
```

Backend GraphQL endpoint is expected at http://localhost:8080/graphql (change in `src/apolloClient.ts` if needed).
